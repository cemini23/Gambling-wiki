# Agents (competition-private)

This public wiki keeps **requirements and entity stubs** for gambling bots. Live arena code stays private so tournament edge is not published here.

**Canonical copy:** private [llm-wiki-by-cemini](https://github.com/cemini23/llm-wiki-by-cemini) → `agents/devfun-poker-arena/` (open **OSINT WORKSPACE** locally).

**Local dev:** clone or symlink from the OSINT workspace:

```bash
ln -sf "../../OSINT WORKSPACE/agents/devfun-poker-arena" agents/devfun-poker-arena
```

Do not commit `decide()` patches, competition briefs, or analyze output here. See the root [README](../README.md) for federation links and support.
